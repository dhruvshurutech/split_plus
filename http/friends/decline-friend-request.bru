meta {
  name: Decline Friend Request
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/friends/requests/{{friendRequestId}}/decline
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

docs {
  # Decline Friend Request
  - Method: POST
  - Path: `/friends/requests/{{friendRequestId}}/decline`
  - Auth: Bearer token required via `Authorization: Bearer {{accessToken}}`
  - Path params: id: UUID (friend request id)
  - Query params: none
  - Body contract: none
  - Response envelope: `{ status: boolean, data?: any, error?: { message: string | string[] } }`
}
